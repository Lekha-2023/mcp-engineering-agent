[build-system]
requires=["setuptools>=68"]
build-backend="setuptools.build_meta"
[project]
name="mcp-engineering-agent"
version="0.1.0"
requires-python=">=3.11"
dependencies=["pydantic>=2.8,<3"]
[project.optional-dependencies]
dev=["pytest>=8,<9"]
[tool.pytest.ini_options]
pythonpath=["."]
